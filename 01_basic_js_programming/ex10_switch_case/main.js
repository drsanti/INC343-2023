/**
 * switch/case
 */

let s = 3;

// Note: Change the value of `s` and check the result in the console window.
switch (s)
{
    case 0: 
        console.log(`case 0`);
        break;

    case 2: 
        console.log(`case 2`);
        break;

    case 7: 
    console.log(`case 7`);
        break;3

    default:
        console.log(`default`);
    break;
}